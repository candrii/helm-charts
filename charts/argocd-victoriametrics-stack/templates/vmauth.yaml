{{- if .Values.vmauth.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.namePrefix }}-vmauth
  namespace: argocd
  labels:
    {{- include "argocd-victoriametrics-stack.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.vmauth.syncWave | quote }}
  finalizers:
    - argoproj.io/resources-finalizer
spec:
  project: {{ .Values.project }}
  source:
    repoURL: {{ .Values.vmauth.chart.repoURL }}
    targetRevision: {{ .Values.vmauth.chart.version }}
    chart: .
    helm:
      {{- if or .Values.vmauth.chart.nameOverride .Values.vmauth.values }}
      values: |
        {{- if .Values.vmauth.chart.nameOverride }}
        nameOverride: {{ .Values.vmauth.chart.nameOverride }}
        {{- end }}
        {{- with .Values.vmauth.values }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
  destination:
    name: {{ .Values.destinationCluster }}
    namespace: {{ .Values.namespaces.stack }}
  syncPolicy:
    {{- toYaml .Values.syncPolicy | nindent 4 }}
{{- end }}

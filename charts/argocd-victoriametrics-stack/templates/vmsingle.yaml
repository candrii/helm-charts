{{- if .Values.vmsingle.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.namePrefix }}-vmsingle
  namespace: argocd
  labels:
    {{- include "argocd-victoriametrics-stack.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.vmsingle.syncWave | quote }}
  finalizers:
    - argoproj.io/resources-finalizer
spec:
  project: {{ .Values.project }}
  source:
    repoURL: {{ .Values.vmsingle.chart.repoURL }}
    targetRevision: {{ .Values.vmsingle.chart.version }}
    chart: .
    helm:
      {{- if or .Values.vmsingle.chart.nameOverride .Values.vmsingle.values }}
      values: |
        {{- if .Values.vmsingle.chart.nameOverride }}
        nameOverride: {{ .Values.vmsingle.chart.nameOverride }}
        {{- end }}
        {{- with .Values.vmsingle.values }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
  destination:
    name: {{ .Values.destinationCluster }}
    namespace: {{ .Values.namespaces.stack }}
  syncPolicy:
    {{- toYaml .Values.syncPolicy | nindent 4 }}
{{- end }}

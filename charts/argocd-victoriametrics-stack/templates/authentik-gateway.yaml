{{- if .Values.authentikGateway.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.namePrefix }}-authentik-gateway
  namespace: argocd
  labels:
    {{- include "argocd-victoriametrics-stack.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.authentikGateway.syncWave | quote }}
  finalizers:
    - argoproj.io/resources-finalizer
spec:
  project: {{ .Values.project }}
  source:
    repoURL: {{ .Values.authentikGateway.chart.repoURL }}
    targetRevision: {{ .Values.authentikGateway.chart.version }}
    chart: .
    helm:
      {{- if or .Values.authentikGateway.chart.nameOverride .Values.authentikGateway.values }}
      values: |
        {{- if .Values.authentikGateway.chart.nameOverride }}
        nameOverride: {{ .Values.authentikGateway.chart.nameOverride }}
        {{- end }}
        {{- with .Values.authentikGateway.values }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
  destination:
    name: {{ .Values.destinationCluster }}
    namespace: {{ .Values.namespaces.stack }}
  syncPolicy:
    {{- toYaml .Values.syncPolicy | nindent 4 }}
{{- end }}

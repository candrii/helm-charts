{{- if .Values.vmalert.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.namePrefix }}-vmalert
  namespace: argocd
  labels:
    {{- include "argocd-victoriametrics-stack.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.vmalert.syncWave | quote }}
  finalizers:
    - argoproj.io/resources-finalizer
spec:
  project: {{ .Values.project }}
  source:
    repoURL: {{ .Values.vmalert.chart.repoURL }}
    targetRevision: {{ .Values.vmalert.chart.version }}
    chart: .
    helm:
      {{- if or .Values.vmalert.chart.nameOverride .Values.vmalert.values }}
      values: |
        {{- if .Values.vmalert.chart.nameOverride }}
        nameOverride: {{ .Values.vmalert.chart.nameOverride }}
        {{- end }}
        {{- with .Values.vmalert.values }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
  destination:
    name: {{ .Values.destinationCluster }}
    namespace: {{ .Values.namespaces.stack }}
  syncPolicy:
    {{- toYaml .Values.syncPolicy | nindent 4 }}
{{- end }}
